#include <bits/stdc++.h>
using namespace std;
#define IOS ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define endl '\n'

void solve(){
    vector<int>v;
    int n; cin>>n;
    while(n--){
        int num; cin>>num;
        v.push_back(num);
   }
    set<int>::iterator it = next(s.begin(),2);
    cout<<*it<<endl;
}

int main(){
    IOS
    int t; cin>>t;
    while(t--)
        solve();
    return 0;
}

